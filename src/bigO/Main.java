package bigO;

public class Main {
	// O(n) - linear time
	// O(n^2) - quadratic time
	// O(1) - constant time
	// O(logn) - logarithmic time
	// O(nlogn) - for some sorting algorithms
	
	// ArrayList time complexity
	// myList = [0, 1, 2, 3]
	// myList.add(3, 4) -> O(1)
	// myList.remove(3) -> O(1)
	// myList.add(0, 4) -> O(n)
	// myList.remove(0) -> O(n)
	// since we measure worst case its O(n)
	
}
